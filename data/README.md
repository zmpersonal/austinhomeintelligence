# Local service-intelligence data

These files are intentionally templates, not fabricated pricing data.

- `vendor-database-template.csv` is the proposed vendor/company schema.
- `service-cost-template.csv` is the proposed normalized cost benchmark schema.

As real Austin companies and quotes are verified, these can feed a future Action Plan engine that compares scope, service area, pricing, licensing/insurance status, response time and customer objectives.
