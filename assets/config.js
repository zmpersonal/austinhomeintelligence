window.AHI_CONFIG = {
  // Deploy worker/worker.js and paste its URL here.
  leadEndpoint: "",

  // Optional. If set, the property view uses Google's official satellite embed.
  // Restrict this browser key to your domain and Maps Embed API.
  googleMapsEmbedKey: "",

  // The current site creates the Action Plan with a transparent rule engine.
  // Later this can be switched when a reviewed AI endpoint is connected.
  actionPlanMode: "rules",

  siteName: "Austin Home Intelligence",
  serviceArea: "Austin, Texas"
};
